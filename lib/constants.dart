import 'package:flutter/material.dart';

const shopName = 'T&R Brechó';

const welcomeBackText = 'Olá, estamos felizes em te ver novamente!';

const welcomeOnBoardText = 'Criar a sua conta leva só um minutinho, vamos lá!';

const productCardTitleStyle = TextStyle(
  color: const Color(0xFFFFFFFF),
  fontWeight: FontWeight.w500,
  shadows: [
    Shadow(
      color: const Color(0xFF000000),
      blurRadius: 8,
    ),
    Shadow(
      color: const Color(0xFF000000),
      blurRadius: 8,
    ),
  ],
);

const productCardPriceStyle = TextStyle(
  color: const Color(0xFFFFFFFF),
  fontWeight: FontWeight.bold,
  shadows: [
    Shadow(
      color: const Color(0xFF000000),
      blurRadius: 8,
    ),
  ],
);

const fallbackImagePath = 'assets/images/pink-tree.jpg';

const firebaseWebApi = 'AIzaSyAVGLCKFLA80LjjZL8Gl9-k5kUvQaRg-tM';

const firebaseDatabaseUrl = 'https://flutter-shop-app-433bb.firebaseio.com/';